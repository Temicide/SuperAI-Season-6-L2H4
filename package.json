{
  "name": "superai-season-6-l2h4",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "clean": "turbo run clean && rm -rf node_modules"
  },
  "packageManager": "npm@11.8.0",
  "devDependencies": {
    "turbo": "^2.4.4"
  }
}
